[package]
name = "hayate-gallery"
version = "0.1.0"
edition = "2024"
description = "Widget gallery / showcase for Hayate UI"
license = "MIT OR Apache-2.0"

[dependencies]
hayate-ui = { path = "../GUI_kit" }
xkbcommon = { version = "0.8", features = ["wayland"] }
